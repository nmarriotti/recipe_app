<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to "Share Recipes", root_path, class:"navbar-brand", style:'font-weight:900;' %>

  <% if user_signed_in? %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%= link_to 'My Recipes', user_recipes_path(current_user.id), class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to 'Create Recipe', new_user_recipe_path(current_user.id), class:"nav-link" %>
        </li>
      </ul>
      <div class="dropdown dropleft">
        <a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= current_user.username %>
        </a>

        <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
          <%= link_to 'Edit Account', edit_user_registration_path, class:"dropdown-item" %>
          <%= link_to 'Sign out', destroy_user_session_path, method: 'delete', class:"dropdown-item" %>
        </div>
      </div>
  <% end %>
</nav>